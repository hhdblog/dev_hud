name: dev_hud
description: A lightweight, draggable, game-focused debug overlay for Flutter.
version: 0.0.5

homepage: https://github.com/hhdblog/dev_hud

repository: https://github.com/hhdblog/dev_hud

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: